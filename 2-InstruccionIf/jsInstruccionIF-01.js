function mostrar()
{
	
	let edad;
	edad = parseInt(document.getElementById("txtIdEdad").value);
	if(edad == 15 ) // no lleva ;
	{
		alert ("Niña Bonita");
	}

	// operadores aritmeticos de tipo number
	// = es anotacion 
	// == es comparacion
	// === comprara hasta tipo ("20" no es 20)
	// != distinto
	// >  mayor
	// <  menor
	// >= mayor o igial
	// <= menor o igual

}