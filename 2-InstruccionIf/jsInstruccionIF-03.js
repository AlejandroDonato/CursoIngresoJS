function mostrar()
{
	let edad;
	edad = parseInt(document.getElementById("txtIdEdad").value);
	if(edad > 17 )
	{
		alert ("es mayor de edad");
	}

	else
	{
		alert ("no es mayor de edad");
	}

	//if si se cumple uma condicion
	//else si no se da la condicion del if
}